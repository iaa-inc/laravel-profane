<?php
return [
    /*
     * If true, validation is skipped when tests are running (app()->runningUnitTests()).
     */
    'skip_on_tests' => false,

    /*
     * If true, validation is always skipped regardless of environment.
     */
    'always_skip' => false,
];